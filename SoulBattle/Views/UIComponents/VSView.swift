import SwiftUI

struct VSView: View {
    var body: some View {
        Text("⚔️")
            .font(.largeTitle)
            .rotationEffect(.degrees(-10))
            .shadow(color: .yellow, radius: 2)
    }
}
